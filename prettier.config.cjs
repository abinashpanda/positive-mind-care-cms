/** @type {import('prettier').Options} */
const config = {
  printWidth: 120,
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
  plugins: ['prettier-plugin-tailwindcss']
}

module.exports = config
